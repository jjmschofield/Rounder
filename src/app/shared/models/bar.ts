export class Bar {
  constructor (public id: number,
               public name: string,
               public lat?: number,
               public lang?: number,
               public imgUrl?: string) {
  }
}
