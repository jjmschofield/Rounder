import { BarModel } from './bar.model'

describe('Shared', () => {
  describe('Models', () => {

    describe('BarModel', () => {

      let underTest: BarModel;

      beforeEach(() => {

      });

      it('should have tests', () => {
        fail();
      });

    });

  });
});

