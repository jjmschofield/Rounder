import {Bar} from './bar'

describe('Shared', () => {
  describe('Models', () => {

    describe('Bar', () => {

      let underTest:Bar;

      beforeEach(() => {

      });

      it('should have tests', () => {
        fail();
      });

    });

  });
});

